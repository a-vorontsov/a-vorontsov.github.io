import Header from './header';

export default class Contact extends React.Component {
    render() {
        return (
            <div>
                <Header/>
                Contact
            </div>
        );
    }
}
