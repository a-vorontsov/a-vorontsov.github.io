import Header from './header';

export default class Projects extends React.Component {
    render() {
        return (
            <div>
                <Header/>
                Projects
            </div>
        );
    }
}
